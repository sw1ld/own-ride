openapi: 3.0.0
info:
  title: OwnRide API
  description: |
    API for analyzing and managing Garmin FIT files. 
    This API supports both JSON for programmatic access and HTML for direct browser viewing.
    
    ![OwnRide API - Sequence diagram](/own/diagrams/api-sequence.svg)
  version: 0.1.0

tags:
  - name: Activities
  - name: Groups
  - name: Statistics
  - name: Upload
  - name: Bikes

paths:
  /own/activities:
    get:
      operationId: getActivities
      summary: 'Fetch all activities'
      description: 'Returns a list of all activities.'
      tags:
        - Activities
      responses:
        '200':
          description: 'A list of activities and groups order by date.'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FeedItem'
            text/html:
              schema:
                type: string
                format: html
  /own/activities/feed:
    get:
      operationId: getFeed
      summary: 'Fetch next fragment of activities.'
      description: 'Returns a list of activities at cursor.'
      tags:
        - Activities
      parameters:
        - name: cursor
          in: query
          schema:
            type: string
            description: 'Base64 encoded cursor pointing to next fragment.'
            example: 'MjAyNi0wNS0wMyAyYjFjNDQ5YS01YjQwLTRlOGItOWNlNi01MmJhMzZlMjNkMWU='
      responses:
        '200':
          description: 'A list of activities and groups order by date starting at cursor.'
          content:
            text/html:
              schema:
                type: string
                format: html
  /own/activities/id/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          example: '6e87e7a4-6326-4e40-933c-750e78f23439'
    get:
      operationId: getActivityById
      summary: 'Fetch an activity by ID'
      tags:
        - Activities
      responses:
        '200':
          description: 'The requested activity.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityResponse'
            text/html:
              schema:
                type: string
                format: html
        '404':
          description: 'Activity with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'
            text/html:
              schema:
                type: string
                format: html
    delete:
      operationId: deleteActivity
      summary: 'Delete an activity by ID'
      tags:
        - Activities
      responses:
        '204':
          description: 'Activity deleted successfully'
        '404':
          description: 'Activity with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'
    put:
      operationId: recalculateActivity
      summary: 'Recalculate an activity using the lastest algorithm.'
      tags:
        - Activities
      responses:
        '200':
          description: 'The updated activity.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityResponse'
        '404':
          description: 'Activity with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'
  /own/activities/id/{id}/name:
    put:
      operationId: updateActivityName
      summary: 'Update the display name of an activity'
      tags:
        - Activities
      requestBody:
        content:
          application/json:
            schema:
              type: string
              example: 'Sunday Morning Ride'
      responses:
        '200':
          description: 'The updated activity.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityResponse'
        '404':
          description: 'Activity with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'
  /own/activities/id/{id}/rate:
    put:
      operationId: rateActivity
      summary: 'Add or overwrite a 0-5 star rating of the chosen activity'
      tags:
        - Activities
      requestBody:
        content:
          application/json:
            schema:
              type: integer
              minimum: 0
              maximum: 5
              example: 4
      responses:
        '200':
          description: 'The updated activity.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityResponse'
        '400':
          description: 'Request body is invalid'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BadRequestProblem'
        '404':
          description: 'Activity with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'

  /own/activities/id/{id}/bike:
    put:
      operationId: setActivityBike
      summary: 'Link or unlink a bike to an activity'
      tags:
        - Activities
      requestBody:
        content:
          application/json:
            schema:
              type: string
              format: uuid
              nullable: true
              example: '016e8bce-d617-40a6-8321-c8d58525042a'
      responses:
        '200':
          description: 'The updated activity.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityResponse'
        '404':
          description: 'Activity with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'

  /own/bikes:
    get:
      operationId: getBikes
      summary: 'Inventory of all bikes'
      tags:
        - Bikes
      responses:
        '200':
          description: 'Inventory of bikes with total driven distance each.'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BikeResponse'
            text/html:
              schema:
                type: string
                format: html
    post:
      operationId: addBike
      summary: 'Add a new bike to the inventory'
      tags:
        - Bikes
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                producer:
                  type: string
                  example: 'Trek'
                name:
                  type: string
                  example: 'Domane'
      responses:
        '201':
          description: 'Bike added to inventory'
          headers:
            Location:
              description: 'URL of the added bike'
              schema:
                type: string
                example: '/own/bikes/id/aac460c9-1246-47e2-9a4c-342b7ad32189'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Bike'
        '303':
          description: 'Redirect to bikes list'
          headers:
            Location:
              description: 'URL to redirect to'
              schema:
                type: string
                example: '/own/bikes'

  /own/bikes/id/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          example: 'aac460c9-1246-47e2-9a4c-342b7ad32189'
    get:
      operationId: getBike
      summary: 'Fetch bike from inventory'
      tags:
        - Bikes
      responses:
        '200':
          description: 'Bike from inventory'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Bike'
        '404':
          description: 'Bike with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'
    put:
      operationId: updateBike
      summary: 'Update a bike'
      tags:
        - Bikes
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                producer:
                  type: string
                name:
                  type: string
      responses:
        '200':
          description: 'Updated bike'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Bike'
        '303':
          description: 'Redirect to bikes list'
          headers:
            Location:
              description: 'URL to redirect to'
              schema:
                type: string
                example: '/own/bikes'
        '404':
          description: 'Bike with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'
    delete:
      operationId: deleteBike
      summary: 'Delete a bike'
      tags:
        - Bikes
      responses:
        '204':
          description: 'Bike deleted'
        '404':
          description: 'Bike with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'

  /own/groups:
    post:
      operationId: createGroup
      summary: 'Create a new activity group'
      tags:
        - Groups
      requestBody:
        content:
          application/json:
            schema:
              type: array
              items:
                type: string
                format: uuid
      responses:
        '201':
          description: 'Group created'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupResponse'
        '400':
          description: 'Request contains unknown Ids'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BadRequestProblem'
      get:
        operationId: fetchGroups
        summary: 'Fetch all groups in an optional time range'
        parameters:
          - name: startDate
            in: query
            schema:
              type: string
              description: 'Optional start date to fetch groups from. Pattern: yyyy-mm-dd'
              example: '2026-08-17'
          - name: endDate
            in: query
            schema:
              type: string
              description: 'Optional end date to fetch groups from. Pattern: yyyy-mm-dd'
              example: '2026-08-17'
        tags:
          - Groups
        responses:
          '200':
            description: 'Ordered list of groups in given time range'
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/GroupResponses'

  /own/groups/id/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getGroupById
      summary: 'Fetch a group by ID'
      tags:
        - Groups
      responses:
        '200':
          description: 'The requested group.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupResponse'
        '404':
          description: 'Group with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'
    put:
      operationId: addActivityToGroup
      summary: 'Add an activity to a group'
      tags:
        - Groups
      requestBody:
        content:
          application/json:
            schema:
              type: string
              format: uuid
      responses:
        '200':
          description: 'Activity added to group'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupResponse'
        '404':
          description: 'Group or activity not found'
    delete:
      operationId: removeActivityFromGroup
      summary: 'Remove an activity from a group'
      tags:
        - Groups
      requestBody:
        content:
          application/json:
            schema:
              type: string
              format: uuid
      responses:
        '204':
          description: 'Activity removed from group'

  /own/groups/id/{id}/name:
    put:
      operationId: updateGroupName
      summary: 'Update group name'
      tags:
        - Groups
      requestBody:
        content:
          application/json:
            schema:
              type: string
              example: 'Hin- und Rückfahrt Lieblingscafe'
      responses:
        '200':
          description: 'Updated group'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupResponse'
        '404':
          description: 'Group with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'

  /own/groups/id/{id}/rate:
    put:
      operationId: rateGroup
      summary: 'Rate a group'
      tags:
        - Groups
      requestBody:
        content:
          application/json:
            schema:
              type: integer
              example: 3
      responses:
        '200':
          description: 'Updated group'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupResponse'
        '400':
          description: 'Request body is invalid'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BadRequestProblem'
        '404':
          description: 'Group with requested ID does not exist'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotFoundProblem'

  /own/stats:
    get:
      operationId: getStatistics
      summary: 'Fetch aggregated statistics for the current year.'
      tags:
        - Statistics
      parameters:
        - name: year
          in: query
          schema:
            type: integer
            example: 2026
      responses:
        '200':
          description: 'Overall statistics for the year.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticResponse'
            text/html:
              schema:
                type: string
                format: html
  /own/upload:
    get:
      operationId: getUploadPage
      summary: 'Get HTML page for uploading a FIT file'
      tags:
        - Upload
      responses:
        '200':
          description: Upload page.
          content:
            text/html:
              schema:
                type: string
                format: html
    post:
      operationId: uploadFile
      summary: 'Upload and persist a FIT file'
      tags:
        - Upload
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
      responses:
        '201':
          description: Upload results.
          headers:
            Location:
              description: 'URL of the first successfully created activity'
              schema:
                type: string
                example: '/own/activities/id/6e87e7a4-6326-4e40-933c-750e78f23439'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadResponseList'
            text/html:
              schema:
                type: string
                format: html

components:
  schemas:
    ActivityResponseList:
      type: array
      items:
        $ref: '#/components/schemas/ActivityResponse'

    ActivityResponse:
      allOf:
        - $ref: '#/components/schemas/FeedItem'
        - type: object
          properties:
            maxSpeed:
              type: string
              nullable: true
              example: '51.95 km/h'
            groupId:
              type: string
              description: 'Optional reference to group.'
              format: uuid

    BadRequestProblem:
      type: object
      properties:
        status:
          type: integer
          example: 400
        title:
          type: string
          example: 'Bad Request'
        detail:
          type: string
          example: "Request body contains invalid values"

    FeedItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          example: '016e8bce-d617-40a6-8321-c8d58525042a'
        type:
          type: string
          description: 'Acts as discriminator controlling additional properties.'
          enum:
            - GROUP
            - ACTIVITY
        displayName:
          type: string
          example: 'Ammerndorf über Tennenlohe Runde'
        date:
          type: string
          description: 'Date or date range'
          example: '2022-06-25'
        distance:
          type: string
          example: '67.46 km'
        duration:
          type: string
          example: '3:19:04'
        elapsedTime:
          type: string
          example: '3:36:22'
        avgSpeed:
          type: string
          example: '20.33 km/h'
        temperature:
          type: string
          example: '26 °C'
        totalAscent:
          type: string
          example: '743 m'
        rate:
          type: integer
          example: 4
        bike:
          $ref: '#/components/schemas/Bike'
        thumbnail:
          type: string
          description: 'SVG content showing the activity on a simple map.'
        positions:
          type: array
          items:
            $ref: '#/components/schemas/Position'

    Group:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        rate:
          type: integer

    GroupResponses:
      type: array
      items:
        $ref: '#/components/schemas/GroupResponse'

    GroupResponse:
      allOf:
        - $ref: '#/components/schemas/FeedItem'
        - type: object
          properties:
            activities:
              type: array
              items:
                $ref: '#/components/schemas/ActivityResponse'

    NotFoundProblem:
      type: object
      properties:
        status:
          type: integer
          example: 404
        title:
          type: string
          example: 'Not Found'
        detail:
          type: string
          example: "Resource with id '6e87e7a4-6326-4e40-933c-750e78f23439' does not exist"

    Position:
      type: object
      properties:
        lat:
          type: number
          example: 49.50781602412462
        lon:
          type: number
          example: 10.907890610396862
        altitude:
          type: number
          example: 294.8
        timestamp:
          type: string
          example: '2022-06-25'

    Bike:
      type: object
      properties:
        id:
          type: string
          format: uuid
          example: 'eb98fb4b-333d-421f-a8b4-03edd5c9826b'
        producer:
          type: string
          example: 'Trek'
        name:
          type: string
          example: 'Domane'

    BikeResponse:
      type: object
      properties:
        bike:
          $ref: '#/components/schemas/Bike'
        totalDistance:
          type: string
          example: '743 km'

    StatisticResponse:
      type: object
      properties:
        rides:
          type: integer
          example: 17
        distance:
          type: string
          example: '821.84 km'
        tourDates:
          type: object
          description: 'Map of dates to distance'
          additionalProperties:
            type: number
            format: double
          example:
            '2026-07-03': 46.77071875
            '2026-07-04': 0.0
            '2026-07-05': 0.0
            '2026-07-06': 39.5099296875

    UploadResponseList:
        type: array
        items:
          $ref: '#/components/schemas/UploadResult'

    UploadResult:
      type: object
      description: 'Result of file upload'
      properties:
        fileName:
          type: string
          example: '2022-06-25_Ammerndorf_über_Tennenlohe_Runde.fit'
        result:
          type: string
          description: 'Acts as discriminator controlling additional properties.'
          enum:
            - SUCCESS
            - FAILURE
          example: 'SUCCESS'
      discriminator:
        propertyName: result
        mapping:
          SUCCESS: '#/components/schemas/UploadSuccess'
          FAILURE: '#/components/schemas/UploadFailure'
      oneOf:
        - $ref: '#/components/schemas/UploadSuccess'
        - $ref: '#/components/schemas/UploadFailure'

    UploadFailure:
      type: object
      description: 'Upload failed'
      properties:
        message:
          type: string
          description: 'Reason for failure'
          example: 'Uploaded file must not be empty!'

    UploadSuccess:
      type: object
      description: 'Upload succeeded'
      properties:
        id:
          type: string
          format: uuid
          description: 'ID of the created activity'
          example: '6e87e7a4-6326-4e40-933c-750e78f23439'
